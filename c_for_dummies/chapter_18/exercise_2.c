#include <stdio.h>
#include <string.h>

int main() {
    char s[] = "Axel";

    printf("string  s: \"%s\"\n", s);
    printf("sizeof(s): %lu\n", sizeof(s));
    printf("strlen(s): %lu\n", strlen(s));

    return (0);
}
